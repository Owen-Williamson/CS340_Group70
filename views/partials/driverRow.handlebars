<tr class="driver-row" data-id="{{id}}" data-name="{{name}}" data-email="{{email}}" data-ld="{{ld}}" data-ed="{{ed}}">
  <th>add</th>
  <th>delete</th>
  <th>{{id}}</th>
  <th>{{name}}</th>
  <th>{{email}}</th>
  <th>{{ld}}</th>
  <th>{{ed}}</th>
</tr>
