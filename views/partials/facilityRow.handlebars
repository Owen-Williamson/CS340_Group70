<tr class="facility-row" data-id="{{id}}" data-c_id="{{customer_id}}" data-location="{{location}}" data-name="{{name}}">
  <th>add</th>
  <th>delete</th>
  <th>{{id}}</th>
  <th>{{customer_id}}</th>
  <th>{{location}}</th>
  <th>{{name}}</th>
</tr>
