<tr class="truck-row" data-id="{{id}}" data-cf-id="{{current_facility_id}}" data-plate="{{plate}}" data-mw="{{max_weight}}" data-mv="{{max_volume}}">
  <th>add</th>
  <th>delete</th>
  <th>{{id}}</th>
  <th>{{current_facility_id}}</th>
  <th>{{plate}}</th>
  <th>{{max_weight}}</th>
  <th>{{max_volume}}</th>
</tr>
